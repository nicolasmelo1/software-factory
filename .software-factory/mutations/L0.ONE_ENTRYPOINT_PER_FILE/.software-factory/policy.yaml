# Generated mutation fixture for L0.ONE_ENTRYPOINT_PER_FILE. It is supposed to fail.
version: 1
project:
  name: mutation-L0.ONE_ENTRYPOINT_PER_FILE
  languages: [python, typescript, go, rust]
docs:
  scan: ["docs/**/*.md"]
gates: {}
rules:
  L0.ONE_ENTRYPOINT_PER_FILE:
    enabled: true
