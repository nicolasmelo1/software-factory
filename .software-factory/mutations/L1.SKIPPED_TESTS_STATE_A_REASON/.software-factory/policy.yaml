# Generated mutation fixture for L1.SKIPPED_TESTS_STATE_A_REASON. It is supposed to fail.
version: 1
project:
  name: mutation-L1.SKIPPED_TESTS_STATE_A_REASON
  languages: [python, typescript, go, rust]
docs:
  scan: ["docs/**/*.md"]
gates: {}
rules:
  L1.SKIPPED_TESTS_STATE_A_REASON:
    enabled: true
