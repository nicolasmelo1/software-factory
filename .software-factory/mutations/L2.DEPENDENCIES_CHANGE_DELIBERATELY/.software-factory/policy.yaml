# Generated mutation fixture for L2.DEPENDENCIES_CHANGE_DELIBERATELY. It is supposed to fail.
version: 1
project:
  name: mutation-L2.DEPENDENCIES_CHANGE_DELIBERATELY
  languages: [python, typescript, go, rust]
docs:
  scan: ["docs/**/*.md"]
gates: {}
rules:
  L2.DEPENDENCIES_CHANGE_DELIBERATELY:
    enabled: true
    options:
        scope: ["package.json"]
        lock_file: ".software-factory/locks/dependencies.lock.json"
