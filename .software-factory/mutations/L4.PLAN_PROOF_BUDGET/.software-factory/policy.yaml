# Generated mutation fixture for L4.PLAN_PROOF_BUDGET. It is supposed to fail.
version: 1
project:
  name: mutation-L4.PLAN_PROOF_BUDGET
  languages: [python, typescript, go, rust, ruby]
docs:
  scan: ["docs/**/*.md"]
gates: {}
rules:
  L4.PLAN_PROOF_BUDGET:
    enabled: true
