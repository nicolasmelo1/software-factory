# Generated mutation fixture for L2.FACTORY_CONFIG_IS_LOCKED. It is supposed to fail.
version: 1
project:
  name: mutation-L2.FACTORY_CONFIG_IS_LOCKED
  languages: [python, typescript, go, rust, ruby]
docs:
  scan: ["docs/**/*.md"]
gates: {}
rules:
  L2.FACTORY_CONFIG_IS_LOCKED:
    enabled: true
