# Generated mutation fixture for L4.PLAN_DECLARES_EXIT_CONDITION. It is supposed to fail.
version: 1
project:
  name: mutation-L4.PLAN_DECLARES_EXIT_CONDITION
  languages: [python, typescript, go, rust, ruby]
docs:
  scan: ["docs/**/*.md"]
gates: {}
rules:
  L4.PLAN_DECLARES_EXIT_CONDITION:
    enabled: true
